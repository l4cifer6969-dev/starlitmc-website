STARLETMC WEBSITE — CORRECTED VERSION

This version intentionally uses ONE index.html file containing all CSS and JavaScript.
That prevents GitHub Pages from failing to load separate styles.css/script.js files.

Upload index.html to the ROOT of your GitHub Pages repository:
- index.html must be directly visible in the repository, not inside another folder.
- Keep your GitHub Pages source as main / (root).

Domain:
starlitmc.fun

Minecraft:
play.starletmc.fun

Discord:
https://discord.gg/starlittt

Store:
Not connected yet. The site shows a professional store section with no fake payment links.

Voting:
Not connected yet. Add your voting links later.

The site automatically checks server status/player count using MCSRVStat.
